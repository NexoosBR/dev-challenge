<% flash.each do |name, msg| %>
  <div class="alert-container">
    <div class="alert alert-top alert-<%= name.to_s == 'notice' ? "success" : "danger" %>">
      <a class="close" data-dismiss="alert">&times;</a>
      <%= msg %>
    </div>
  </div>
<% end %>