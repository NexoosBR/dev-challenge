<%= form_with(model: loan_applicant, local: true) do |form| %>
  <% if loan_applicant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loan_applicant.errors.count, "error") %> prohibited this loan_applicant from being saved:</h2>

      <ul>
      <% loan_applicant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company_name %>
    <%= form.text_field :company_name %>
  </div>

  <div class="field">
    <%= form.label :company_number %>
    <%= form.text_field :company_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
