<div class="table-content">
  <table class="table table-striped">
    <thead>
      <tr>
        <th scope="col">Razão Social</th>
        <th scope="col">CNPJ</th>
        <th scope="col"></th>
      </tr>
    </thead>
    <tbody>
      <% @companies.each do |company| %>
          <tr>
            <td><%= company.name %></td>
            <td><%= company.cnpj %></td>
            <td><%= link_to 'Ver detalhes', company_path(company) , class: 'btn btn-info' %></td>
          </tr>
      <% end %>
    </tbody>
  </table>
</div>
